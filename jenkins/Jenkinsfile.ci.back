pipeline {
    agent any

    tools {
        nodejs 'NodeJS'
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Install Backend') {
            steps {
                dir('BackOffice') {
                    bat 'npm install'
                }
            }
        }

        stage('Test Backend') {
            steps {
                dir('BackOffice') {
                    bat 'npm run test:cov -- --passWithNoTests --forceExit'
                }
            }
        }

        stage('Build Backend') {
            steps {
                dir('BackOffice') {
                    bat 'npm run build'
                }
            }
        }
    }

    post {
        success {
            echo '✅ CI Backend réussi'
        }

        failure {
            echo '❌ CI Backend échoué'
        }
    }
}
